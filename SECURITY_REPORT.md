# گزارش امنیتی (SECURITY_REPORT.md)

این گزارش تدابیر امنیتی اتخاذ شده در نسخه PHP پروژه را تشریح می‌کند.

## ۱. مقابله با SQL Injection
- تمام تعاملات با پایگاه داده از طریق **PDO** و **Prepared Statements** انجام می‌شود.
- هیچ متغیری به طور مستقیم در رشته‌های SQL قرار نمی‌گیرد.

## ۲. مقابله با XSS (Cross-Site Scripting)
- ورودی‌های کاربر قبل از پردازش یا ذخیره‌سازی، توسط تابع `Helpers::sanitize` (شامل `htmlspecialchars` و `strip_tags`) پاکسازی می‌شوند.
- در خروجی‌های JSON، هدر `Content-Type: application/json` اجباری است که مانع از اجرای اسکریپت در مرورگر می‌شود.

## ۳. امنیت احراز هویت
- پسوردها با استفاده از الگوریتم **Argon2** یا **bcrypt** (از طریق `password_hash` بومی PHP) ذخیره می‌شوند.
- نشست‌ها مبتنی بر **JWT** با کلیدهای رمزنگاری قوی (حداقل ۳۲ کاراکتر) هستند.
- توکن‌های Refresh در دیتابیس ذخیره شده و امکان ابطال (Revoke) دارند.

## ۴. امنیت فایل‌ها و آپلود
- پوشه `uploads/` دارای فایل `.htaccess` برای جلوگیری از اجرای اسکریپت‌های PHP است (`php_flag engine off`).
- پسوندهای مجاز فایل محدود شده‌اند (فقط تصاویر و اسناد رایج).
- حجم فایل‌ها محدود به ۱۰ مگابایت است.
- نام فایل‌ها برای جلوگیری از تداخل و افشای اطلاعات، به صورت تصادفی تغییر می‌یابد.

## ۵. هدرهای امنیتی (Secure Headers)
هدرهای زیر در تمام پاسخ‌های API تنظیم شده‌اند:
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `X-XSS-Protection: 1; mode=block`
- `Strict-Transport-Security` (در صورت فعال بودن HTTPS)
- `Content-Security-Policy`

## ۶. محدودیت دسترسی
- مسیرهای حساس `/api/admin/*` توسط میان‌افزار `requireAdmin` محافظت می‌شوند که اعتبار توکن JWT و نقش کاربر را بررسی می‌کند.
